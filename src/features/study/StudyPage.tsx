/** Populated with the full authored guide in a later phase — routing/navigation land first. */
export function StudyPage() {
  return (
    <div className="mx-auto flex max-w-content flex-col gap-4 p-6">
      <h1 className="text-2xl font-semibold text-text-primary">Study</h1>
      <p className="text-text-muted">The interview study guide is being authored — check back soon.</p>
    </div>
  );
}
