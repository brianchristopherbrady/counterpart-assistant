import { Dialog } from "@microsoft/fast-foundation";

/** Focus trap, Escape-to-dismiss, and overlay-click "dismiss"/"cancel" events come from the foundation class as-is. */
export class DsDialogElement extends Dialog {}
