import { statusMessageTemplate } from "./status-message.template";
import { statusMessageStyles } from "./status-message.styles";

export const statusMessageDefinition = {
  name: "ds-status-message",
  template: statusMessageTemplate,
  styles: statusMessageStyles,
};
