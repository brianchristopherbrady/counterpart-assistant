import { Radio } from "@microsoft/fast-foundation";

/** Accessible radio semantics (checked state, keyboard activation) come from the foundation class as-is. */
export class DsRadioElement extends Radio {}
