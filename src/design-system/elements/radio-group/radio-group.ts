import { RadioGroup } from "@microsoft/fast-foundation";

/** Accessible radiogroup semantics (roving tabindex, arrow-key navigation) come from the foundation class as-is. */
export class DsRadioGroupElement extends RadioGroup {}
